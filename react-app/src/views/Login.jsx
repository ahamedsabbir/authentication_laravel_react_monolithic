import React from 'react'

function Login() {
  /* import { loginSuccess, loginFailure, logout, updateUser } from './reducers/reducerAuth';

// On successful login
dispatch(loginSuccess(userData, token));

// On login failure
dispatch(loginFailure(error));

// To log out
dispatch(logout());

// To update user data
dispatch(updateUser(newUserData)); */
  return (
    <>
    <h1>hello</h1>
    </>
  )
}

export default Login